{% extends 'base.html.twig' %}

{% block title %}Illustration{% endblock %}

{% block body %}
    <div class="d-flex justify-content-center mt-5">
        <img src="{{ asset('uploads/') ~ illustration.nomFichier }}" alt="images astronomie">
    </div>

    <div class="row">
        <div class="col-6">
        {% if is_granted("ROLE_ADMIN") %}
            <button class="btn btn-info mt-3"><a href="{{ path('illustration_index') }}">Retour à l'index</a></button>
            {{ include('illustration/_delete_form.html.twig') }}
        {% endif %}
        </div>
    </div>
{% endblock %}
