<form method="post" action="{{ path('illustration_delete', {'id': illustration.id}) }}" onsubmit="return confirm('Voulez-vous supprimer cette illustration ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ illustration.id) }}">
    <button class="btn btn-danger mt-3">Supprimer</button>
</form>
