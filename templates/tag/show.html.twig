{% extends 'base.html.twig' %}

{% block title %}Tag{% endblock %}

{% block body %}
    {# <h1>Tag</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tag.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ tag.nom }}</td>
            </tr>
        </tbody>
    </table> #}

    <div class="d-flex justify-content-center mt-5">
        <h1>
            {{ tag.nom }}
        </h1>
    </div>
    {# TODO : demander si il y a que le moyen css classique pour faire du margin plus grand #}


    {# TODO demander si c'est une bonne idée d'enlever ce bouton pour pas que les membres le voit #}
    {% if is_granted("ROLE_ADMIN") %}
        <button class="btn btn-info"><a href="{{ path('tag_index') }}">Retour</a></button>
        <button class="btn btn-info"><a href="{{ path('tag_edit', {'id': tag.id}) }}">Modifier</a></button>
        {# <button class="btn btn-info">{{ include('article/_delete_form.html.twig') }}</button> #}
    {% endif %}

    {# {{ include('tag/_delete_form.html.twig') }} #}
{% endblock %}
